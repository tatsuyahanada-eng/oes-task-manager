# DB Controller — PHP 版

レンタルサーバの **中で動く** DB 管理ツール。

Node.js も、常駐プロセスも、SSH トンネルも要りません。
PHP が動くサーバに置けば、ブラウザからそのまま使えます。

---

## なぜ PHP 版があるのか

Node.js 版は、レンタルサーバでは動きません（常駐プロセスが必要なため）。
そのため手元の PC で動かすことになり、レンタルサーバの MySQL が
`mysqlXXX.phy.lolipop.lan` のような**内部専用のホスト名**だと届かず、
SSH トンネルが必要になっていました。

```
Node.js 版:  手元の PC ──SSH トンネル──▶ レンタルサーバ ──▶ MySQL
PHP 版:      ブラウザ ──HTTPS──▶ レンタルサーバ内のツール ──▶ MySQL（隣）
```

**PHP 版はサーバの中で動くので、内部ホスト名にそのまま届きます。**
トンネルという回り道が丸ごと不要になります。

---

## できること

| | |
|---|---|
| 接続先の登録 | 画面から。何件でも。パスワードは AES-256-GCM で暗号化して保存 |
| DB の切り替え | ツリーの上部で切り替え |
| テーブル構成の確認 | ツリー表示。列の型・主キー・索引・外部キー・コメント |
| データの参照 | 絞り込み（WHERE）、並び替え、ページング |
| データの追加・修正・削除 | GUI で 1 行ずつ。実行前に SQL を提示して確認 |
| 参照系 SQL の実行 | CUI から `SELECT` / `SHOW` / `EXPLAIN` |
| 利用者の管理 | 管理者 / 運用者 / 閲覧者の 3 段階 |
| 操作履歴 | 誰がいつ何をしたかを記録 |
| PWA | スマホのホーム画面に追加できる |

### まだ無いもの

| | 理由 |
|---|---|
| CSV の入出力 | 未実装。Node.js 版にはあります |
| 更新系 SQL の自由入力 | 意図的に外しています。行の操作は画面から |
| Oracle / SQL Server | PHP の拡張（oci8 / sqlsrv）が共用サーバにほぼ無いため |
| PostgreSQL | `pdo_pgsql` があれば動く想定ですが、**未検証です** |

---

## 置き方

### 1. アップロードする

`public_html` の下に、**推測されにくい名前**のフォルダを作って、丸ごと置きます。

```
public_html/
└── dbc-x8f3k9/              ← 推測されにくい名前にする
    ├── .htaccess
    ├── index.php
    ├── lib/
    ├── public/
    └── data/                ← 空でよい。自動で作られる
```

### 2. 権限を整える

```bash
chmod 700 data
chmod 644 index.php .htaccess
chmod 755 lib public
```

### 3. 開く

```
https://例.com/dbc-x8f3k9/
```

ログイン画面が出ます。

```
初期ユーザー名 : welsysadm
初期パスワード : Welsys@1234
```

### 4. すぐにパスワードを変える

**インターネットに公開されている状態です。** ログインしたら、
「設定」→「パスワードを変更」から必ず変更してください。

---

## 置いた直後に必ず確認すること

`data/` の中には、接続先のパスワードを復号する鍵が入っています。
これが読めてしまうと、すべてが台無しです。

```bash
# すべて 403 か 404 が返れば正しい
curl -s -o /dev/null -w '%{http_code}\n' https://例.com/dbc-x8f3k9/data/auth.json
curl -s -o /dev/null -w '%{http_code}\n' https://例.com/dbc-x8f3k9/data/connections.json
curl -s -o /dev/null -w '%{http_code}\n' https://例.com/dbc-x8f3k9/data/.masterkey
curl -s -o /dev/null -w '%{http_code}\n' https://例.com/dbc-x8f3k9/lib/crypto.php

# 認証なしで API が叩けないこと（401 が返れば正しい）
curl -s -o /dev/null -w '%{http_code}\n' https://例.com/dbc-x8f3k9/api/connections
```

**1 つでも 200 が返ったら、すぐにフォルダごと削除してください。**

`.htaccess` で二重に塞いでいますが、サーバの設定によっては
`.htaccess` が無効なことがあります。必ず実際に確かめてください。

より確実にするなら、`data/` を `public_html` の外へ移し、
`lib/util.php` の `data_path()` が指す先を変えてください。

---

## 安全のしくみ

**接続ごと**

- 既定で **読み取り専用**。解除しない限り、誰であっても変更できない
- パスワードは AES-256-GCM で暗号化して保存。画面へは一切返さない

**利用者ごと**

| 役割 | 利用者の管理 | 接続先の登録・変更 | データの変更 | 参照 |
|---|:---:|:---:|:---:|:---:|
| 管理者 admin | ○ | ○ | ○ | ○ |
| 運用者 operator | × | × | ○ | ○ |
| 閲覧者 viewer | × | × | × | ○ |

**両方を満たさないと書き込めません。**

**更新の実行時**

- 対象テーブルに主キーがあること。無ければ変更させない
- ビューは変更させない
- トランザクション内で実行し、**影響行数がちょうど 1 行**でなければロールバック
- 値はすべてバインド変数として渡す（`PDO::ATTR_EMULATE_PREPARES = false`）
- WHERE 欄はセミコロン・コメント記法・更新系の語を弾く
- 自由入力の SQL は参照系だけ

**ログイン**

- パスワードは `password_hash` でハッシュ化して保存
- Cookie は httpOnly / SameSite=Strict、HTTPS なら Secure
- 無操作 30 分、発行から 12 時間で切れる
- 同じ相手から 5 回失敗すると 15 分受け付けない
- ログインのたびにセッション ID を作り直す（セッション固定攻撃対策）
- 役割の変更・利用停止は、開いたままの画面にも次の操作から効く

---

## 動作確認の状況

MariaDB 10.11 に対して、実際に確認しています。

| 内容 | 件数 |
|---|---|
| 権限による API の拒否 | 31 |
| 安全装置（読取専用・主キー無し・ビュー・複数行一致・SQL 注入・暗号化） | 24 |
| 実 DB での参照・追加・修正 | 13 |
| 画面表示（ツリー・列定義・日本語・日付） | 15 |
| **ブラウザからの追加・修正・削除** | 18 |

確認できている具体的な項目:

- ハイフンを含む DB 名（`LAA1234567-shop`）
- 日本語のテーブルコメント・列コメント
- 日本語データの読み書き（化けない）
- `DATE` がタイムゾーンでずれない
- **値が変わらない `UPDATE` も 1 行として数える**（MySQL 特有）
- 主キーが 2 行に当たる `UPDATE` を検出してロールバック
- `WHERE` 欄への SQL 注入の試み（セミコロン・コメント・`DELETE`）を拒否
- 接続パスワードが `connections.json` に平文で残らない

**ロリポップの実サーバでは未確認です。** 手元の MariaDB での確認です。
